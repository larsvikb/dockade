# Control-plane runtime deps. Pinned for reproducible builds. This is the
# management plane, not the egress security choke point, so the pinning bar is a
# notch below proxies/egress (no digest required) — but still deliberate.
fastapi==0.141.1
uvicorn==0.53.0
