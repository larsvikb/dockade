# Identity ([user] name/email) is NOT set here — it is per-person and would make
# the image host-specific. The launcher (sandbox-lib.sh) reads it from the host's
# git config at launch and the entrypoint materializes it. See DESIGN.md.
[init]
	defaultBranch = main
[pull]
	rebase = true
