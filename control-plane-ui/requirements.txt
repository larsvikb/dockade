# Control-plane UI (frontend) deps. Pinned for reproducible builds. A dumb
# reverse proxy + static server, not a security boundary, so the pinning bar
# matches the backend (no digest required) but is still deliberate.
fastapi==0.141.1
uvicorn==0.53.0
httpx==0.28.1
