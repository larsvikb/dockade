# Egress allowlist for the governed proxy (default-deny; anything not listed is
# blocked and audited). One host per line. A leading dot matches subdomains
# (".github.com" => api.github.com, codeload.github.com, ...); a bare line is an
# exact host match. Blank lines and #-comments are ignored. Re-read on every
# connection, so edits apply to the next one.
#
# Grouped by target-state disposition, mirroring init-firewall.sh. This is the
# static, file-based precursor to the control plane's dynamic policy store.

# PERMANENT — the agent's own API / auth lifeline.
api.anthropic.com
claude.ai
platform.claude.com

# TRANSITIONAL — package registries. These move behind the pull-through cache
# (whose upstream flows through this proxy) when it lands; remove them then.
registry.npmjs.org
pypi.org
files.pythonhosted.org

# TRANSITIONAL — GitHub (git clone/pull/push, gh, API). Governed by NAME here,
# where the v1 firewall could only allow GitHub's IP ranges. Moves behind the
# governed git path in the target design.
github.com
.github.com
codeload.github.com
objects.githubusercontent.com
raw.githubusercontent.com
